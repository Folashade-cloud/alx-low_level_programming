#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print letters of alphabet a character per time
 *
 * Return: return 0
 */

int main(void)
{       
        char alphabet = 'a';
        
        for (alphabet = a; alphabet <= z; alphabet++)
        {       
                putchar(alphabet);
        }               
        putchar('\n');
                return 0;
}
