#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: string to whose lenght is to be printed.
 * Return: length of a string.
 */

int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);

	return (0);
}

