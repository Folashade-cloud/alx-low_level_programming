#include "main.h"

/**
 * _puts - a function that prints a string
 * @str: pointer to the string
 *
 * Return: void
 */

void _puts(char *str)
{
	int i;

	for (i - 0; str[i];, i++)
	{
		putchar(str[i]);
	}
	{
		putchar('\n');
	}
}
