#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings.
 * @s1: parameter1
 * @s2: parameter2
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
int compare;
compare = strcmp(s1, s2);
return (compare);
}
