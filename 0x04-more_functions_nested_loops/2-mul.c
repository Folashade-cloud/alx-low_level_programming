#include "main.h"

/**
 * mul -function that multiplies two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: result of the multiplication of a and b
 */

int mul(int a, int b)
{
	int mul_answer;

	mul_answer = a * b;
	return (mul_answer);
}
